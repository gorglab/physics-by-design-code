# Physics by Design - pinned Python dependencies for reproducing every
# script-generated STL in this paper.
#
# Tested with CPython 3.10 and 3.11 on Linux (Ubuntu 22.04, x86_64).
# Pinned to the minor version that produced the figures in main.pdf.
#
# Install:  python3 -m venv .venv && source .venv/bin/activate
#           pip install -r requirements.txt

numpy>=1.24,<3
scipy>=1.10,<2
matplotlib>=3.7,<4
scikit-image>=0.21,<1
trimesh>=4.0,<5
pillow>=10.0,<12
numpy-stl>=3.1,<4
